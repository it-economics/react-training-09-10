import { Stack, Typography } from '@mui/material';

export const NotFound = () => <Stack>
  <Typography variant={"h1"}>404 - Not found!</Typography>
  <Typography variant={"h2"}>This is not the page you are looking for!</Typography>
</Stack>
